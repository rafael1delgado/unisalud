<?php

use Illuminate\Database\Seeder;
use App\Models\MedicalProgrammer\Activity;

class HmUpdateActivitiesTableSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // $activity = App\Post::where('id', 1)->first();
        // $activity->description = "Es la primera atención generada por un episodio de una enfermedad, mediante la que se puede o no dar resolución a un problema de salud. Cada primera atención de un paciente generado por una interconsulta es una consulta nueva por la especialidad que realiza la atención, independiente que el paciente haya sido atendido en otra ocasión en la especialidad.";
        // $activity->save();
        //
        // $activity = App\Post::where('id', 2)->first();
        // $activity->description = " Son las atenciones posteriores a la primera consulta por un mismo problema de salud cuya finalidad es completar el estudio, tratamiento y/o seguimiento.";
        // $activity->save();
        //
        // $activity = App\Post::where('id', 3)->first();
        // $activity->description = "";
        // $activity->save();
        //
        // $activity = App\Post::where('id', 2)->first();
        // $activity->description = "";
        // $activity->save();
        //
        // $activity = App\Post::where('id', 2)->first();
        // $activity->description = "";
        // $activity->save();
        //
        // $activity = App\Post::where('id', 2)->first();
        // $activity->description = "";
        // $activity->save();
        //
        // $activity = App\Post::where('id', 2)->first();
        // $activity->description = "";
        // $activity->save();
    }
}
